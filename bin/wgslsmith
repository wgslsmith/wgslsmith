#!/usr/bin/env bash

script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)
project_dir=$(dirname "$script_dir")

export WGSLSMITH_ROOT="$project_dir"

"$project_dir/target/release/wgslsmith" "$@"
